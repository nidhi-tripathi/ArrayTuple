///Tuple in TS//
//A tuple is a finite ordered list of elements,where each element can have a different type.
//tuple have a fixed length.
//tuples are represented using square brackets, and each element is separated by a comma. 
//tuple create a limit on his length.each element have define specific type 

let role:[string,string,number,number,boolean?] = ["admin","manager",1,3];
role.push(true);
role[1]="user";
